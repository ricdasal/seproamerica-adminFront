import { DirectivaUsuariosDirective } from './directiva-usuarios/directiva-usuarios.directive';

describe('DirectivaUsuariosDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaUsuariosDirective();
    expect(directive).toBeTruthy();
  });
});
